<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Packages Base Path
    |--------------------------------------------------------------------------
    |
    | This is where new packages will be generated relative to your Laravel
    | application base path.
    |
    */

    'packages_path' => 'packages',

    /*
    |--------------------------------------------------------------------------
    | Default Vendor (Optional)
    |--------------------------------------------------------------------------
    |
    | If set, this value could be used as a suggested default vendor when
    | prompting in the interactive command. By default it is null so that
    | no vendor is ever pre-filled.
    |
    */

    'default_vendor' => null,

    /*
    |--------------------------------------------------------------------------
    | Default License
    |--------------------------------------------------------------------------
    |
    | The license identifier to use in generated packages. The actual text is
    | taken from the LICENSE stub.
    |
    */

    'default_license' => 'MIT',
];

